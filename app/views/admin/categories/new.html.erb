<% @page_title = "新規カテゴリ登録"%>

<h2><%= @page_title %></h2>
<div class="paragraph">
    <p>
        新しいカテゴリを登録することができます
    </p>
</div>

<%= render "shared/errors", obj: @category %>

<%= form_for @category, url: admin_categories_path do |f| %>
    <div class="category_form">
        <%= f.text_field :category_name %>
        <div class="submit"><%= f.submit "登録" %></div>
    </div>
<% end %>